"use strict";
var app;

function loadVue() {
	app = new Vue({
	    el: "#app",
	    data: {
            Decimal,
            game,
            player,
            tmp,
			format,
            formatTime,
            switchTab,
            TABS_LIST,
            tab,
            otherGameStuffIg,
            DEFAULT_SCALE,
            gRC,
            colorChange,
            buyGenUPG,
            reset,
            ACHIEVEMENT_DATA,
            ACH_DEF_COLORS,
            c
        }
	})
}