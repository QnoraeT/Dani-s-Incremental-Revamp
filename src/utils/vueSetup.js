var app;

function loadVue() {
    console.log(FPS)
	app = new Vue({
	    el: "#app",
	    data: {
            Decimal,
            game,
            player,
			format,
            tmp,
            TABS_LIST,
            tab,
            switchTab,
            FPS
        }
	})
}